export default function(values) {
    return values.reduce((total, val) => total + val, 0)
}

export function sumValues(values) {
    return values.reduce((total, val) => total + val, 0)
}

export function sumValues2(values) {
    return values.reduce((total, val) => total + val, 0)
}